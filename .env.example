# RFQ form email delivery — copy to .env.local (dev) or set in your host.
# Without SMTP_HOST, submissions are logged server-side instead of emailed.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
RFQ_TO_EMAIL=info@operon.co
RFQ_FROM_EMAIL=
